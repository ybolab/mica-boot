_out/
.tmp/
tmp/

# The build substrate, fetched at its pin by tools/deps.sh from the releases
# of ybolab/mica-build-env (deps/sources/mica-build-env.json).
/build-env/
# The pinned Debian base the boot-tools image installs from (mica-debian).
/debian/
# A signing workspace, if one is generated here for the standalone gates.
/meta/
